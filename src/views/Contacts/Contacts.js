import React, { useEffect } from 'react';
import { View, Text } from 'react-native';
import link from 'react-native-contacts';
const Contacts = () => {
    // console.log(link)
    // link.getAll().then(res => {
    //     console.log(res)
    // })
    return (
        <View>
            <Text>在模拟器执行会闪退，</Text>
        </View>
    );
}

export default Contacts;